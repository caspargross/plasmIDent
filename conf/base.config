/*
 *---------------------------------
 * PLASMID IDENT BASE CONFIGURATION
 *---------------------------------
 * Default config for all environments
 */


params {
    outDir = 'PlasmIdent'
    maxLength = 500000 // Maximum contig length for putative plasmid
    minLength = 1000 // Minimal length
    targetCov = 50 // Filter read file to reach this target coverage
    seqPadding = 1000  // Alignment overlap length at contig edges
    covWindow = 50  // Window size for coverage calculation
    cpu = 4
}

// Safe bash settings
process.shell = ['/bin/bash', '-e']


report {
// Create html report 
    enabled = true
    file = "${params.outDir}/nf_reports/PlasmIdent_Report.html"
}
dag {
// Draw graph structure of pipeline
    enabled = true
    file = "${params.outDir}/nf_reports/PlasmOdent_Flowchart.svg"
}

/*
timeline {
// Turn on timeline tracking
    enabled = true
    file = "${params.outDir}/nf_reports/PlasmIdent_timeline.html"
}
trace {
// Turn on trace tracking 
    enabled = true
    file = "${params.outDir}/nf_reports/PlasmIdent_Trace.txt"
}
*/
